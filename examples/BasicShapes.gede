ramp 1;

code

($v1%64/64)*0.1 ($v1/t%4==0)*2 1,

($v1%64<32)*0.1 ($v1/t%4==1)*3 2,

sin($v1/7)*0.1 ($v1/t%4==2)*4 2,

sin($v1*sin($v1))*0.1 ($v1/t%4==3)*8 2,

end;

Take a look at the expressions before *0.1.

1. SAW WAVE $v1%x/x
2. SYMETRIC SQUARE WAVE $v1%x<(x/2)
3. SINE WAVE sin($v1/x)
4. NOISE sin($v1*sin($v1))

*0.1 is the amplitude and $v1/t%4==X is the boolean trigger and *2 *3 *4 the subdiv.